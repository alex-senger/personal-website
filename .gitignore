# Build output
node_modules/
dist/
.astro/

# Local tooling and knowledge-graph output (contains extracted personal data)
.claude/
graphify-out/

# CV sources and compiled PDFs — kept out of the public repo because they
# contain private contact details. Compile into public/downloads/ locally;
# in production the directory is bind-mounted into the container.
cv/
public/downloads/

# Misc
npm-debug.log*
.DS_Store
.env
.env.*
